# openspec/config.yaml — OptiClusters v4.0.0
# Auto-generated by sdd-init. Edit to match project evolution.
schema: spec-driven

context: |
  C++17 desktop app with CMake 3.20+, GCC 16.1.1 (Linux) + MinGW cross (Windows).
  Stack: GLFW 3.4 windowing, ImGui docking branch for UI, OpenGL 3.3 Core Profile,
  miniaudio (header-only) for audio synthesis, nlohmann/json v3.11.3 for persistence.
  Architecture: monolithic header-only design. Core domain in src/nucleo/ (Grafo,
  SimuladorRed, algoritmos namespace, tipos structs). Entire UI lives in
  src/interfaz/Interfaz.h (~2500 lines). No tests, no CI, no linter/formatter.
  Naming: CamelCase classes/namespaces, snake_case members, Spanish UI strings.
  Dead files: src/nucleo/Grafo_old.h (543 lines, legacy).
  Empty dirs: src/aplicacion/, src/interfaz/paneles/, src/interfaz/dialogos/.

rules:
  proposal:
    - Include effort estimate and risk assessment for changes touching Interfaz.h
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Document architecture decisions with rationale
    - Prefer extracting from huge headers before adding new code
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (Spanish UI strings, #pragma once, header-only style)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "cmake --build build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  runner: none
  framework: ""
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  strict_tdd: false

project: OptiClusters
version: 4.0.0
language: C++17
build_system: cmake
frameworks: [glfw, imgui, opengl3, miniaudio, nlohmann_json]
stack: desktop
